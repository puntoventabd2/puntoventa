CREATE OR REPLACE
FUNCTION fn_Obtener_Precio(id_produ IN NUMBER) 
RETURN NUMBER
IS
  result NUMBER;
BEGIN
  SELECT PRECIO INTO result
  FROM PRODUCTOS
  WHERE PRODUCTO = id_produ;
  return(result);
EXCEPTION 
WHEN NO_DATA_FOUND THEN
  return 0;
END ;
