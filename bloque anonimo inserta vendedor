--INGRESA UN NUEVO VENDEDOR

DECLARE
BEGIN
  INSERT INTO VENDEDOR
  VALUES(&ID_VENDEDOR, &ID_TIENDA, &NOMRE, &APEPATER, &APEMATER, &DIRECCION, &TELEFONO, &EMAIL, &FECHANACIMIENTO, &FECHAINGRES, &GENERO, &CURP,&RFC);
  
  DBMS_OUTPUT.PUT_LINE('SE INSERTO UN NUEVO VENDEDOR');
  
END;
